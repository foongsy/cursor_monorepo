ENV=development
PORT=8000
CORS_ORIGINS=["http://localhost:5173", "https://your-frontend-url.onrender.com"]
# ↑ Parsed by Pydantic Settings as List[str] — accepts JSON array OR comma-separated values
# Alternative format (comma-separated): CORS_ORIGINS=http://localhost:5173,https://your-frontend-url.onrender.com
# Note: For URLs with commas/special chars, use JSON array format with proper escaping; no quotes needed for simple comma-separated values

